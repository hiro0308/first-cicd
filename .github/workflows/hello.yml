name: Hello
on: push
jobs:
  hello:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
    steps:
      - run: echo "hello, world"
      - run: echo "${BRANCH}"
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH }}
